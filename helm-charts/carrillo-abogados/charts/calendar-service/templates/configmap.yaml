apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "calendar-service.fullname" . }}-config
  labels:
    {{- include "calendar-service.labels" . | nindent 4 }}
data:
  application.yml: |
    spring:
      application:
        name: calendar-service
    server:
      port: {{ .Values.service.targetPort }}
    management:
      endpoints:
        web:
          exposure:
            include: health,info,prometheus,metrics