apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "client-service.fullname" . }}-config
  labels:
    {{- include "client-service.labels" . | nindent 4 }}
data:
  application.yml: |
    spring:
      application:
        name: client-service
    server:
      port: {{ .Values.service.targetPort }}
    management:
      endpoints:
        web:
          exposure:
            include: health,info,prometheus,metrics