apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "case-service.fullname" . }}-config
  labels:
    {{- include "case-service.labels" . | nindent 4 }}
data:
  application.yml: |
    spring:
      application:
        name: case-service
    server:
      port: {{ .Values.service.targetPort }}
    management:
      endpoints:
        web:
          exposure:
            include: health,info,prometheus,metrics