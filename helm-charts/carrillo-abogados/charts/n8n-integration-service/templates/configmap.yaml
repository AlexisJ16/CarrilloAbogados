apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "n8n-integration-service.fullname" . }}-config
  labels:
    {{- include "n8n-integration-service.labels" . | nindent 4 }}
data:
  application.yml: |
    spring:
      application:
        name: n8n-integration-service
    server:
      port: {{ .Values.service.targetPort }}
    management:
      endpoints:
        web:
          exposure:
            include: health,info,prometheus,metrics